import React from 'react';
import { Box, Typography, Paper } from '@mui/material';

// Placeholder components for all the main pages
export const Register: React.FC = () => (
  <Box><Typography variant="h4">ثبت نام</Typography></Box>
);

export const Checks: React.FC = () => (
  <Box><Typography variant="h4">مدیریت چک‌ها</Typography></Box>
);

export const Employees: React.FC = () => (
  <Box><Typography variant="h4">مدیریت کارکنان</Typography></Box>
);

export const Payroll: React.FC = () => (
  <Box><Typography variant="h4">حقوق و دستمزد</Typography></Box>
);

export const Customers: React.FC = () => (
  <Box><Typography variant="h4">مدیریت مشتریان</Typography></Box>
);

export const Invoices: React.FC = () => (
  <Box><Typography variant="h4">مدیریت فاکتورها</Typography></Box>
);

export const Products: React.FC = () => (
  <Box><Typography variant="h4">مدیریت محصولات</Typography></Box>
);

export const Reports: React.FC = () => (
  <Box><Typography variant="h4">گزارشات</Typography></Box>
);